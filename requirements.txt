# Core dependencies (minimal per assignment)
torch>=2.0.1
numpy>=1.24.0
pyyaml>=6.0.1
tensorboard>=2.14.0
einops>=0.7.0
dataclasses-json>=0.6.0

# Utilities (optional: wandb can be disabled in config)
tqdm>=4.65.0
wandb>=0.16.0
omegaconf>=2.3.0
gym>=0.26.0